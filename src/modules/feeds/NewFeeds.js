import * as React from 'react';
import {View, Text, Button} from 'react-native';

function NewFeeds({navigation}) {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>NewFeeds</Text>
    </View>
  );
}
export default NewFeeds;
